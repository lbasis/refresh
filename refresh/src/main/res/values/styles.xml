<resources>

    <style name="re_progress_default_style">
        <item name="android:indeterminateOnly">true</item>
        <item name="android:minWidth">@dimen/re_progress_size</item>
        <item name="android:maxWidth">@dimen/re_progress_size</item>
        <item name="android:minHeight">@dimen/re_progress_size</item>
        <item name="android:maxHeight">@dimen/re_progress_size</item>
    </style>
</resources>
